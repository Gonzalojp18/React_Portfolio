Los objetos no sin hijos de react. Por ende si se la pasa un objeto {} dara un error.

Si se la pasa una function, no podemos olvidar los parentesis para el llamado de la funcion ejemplo:

() ejecuta otra function

() => elemento()

JERARQUIA DE COMPONENTES.

Cada video genera un titulo, fecha, descripcion

Video; se cambiarian algunos parametros



ESTILOS:

Cuando se actualiza un estado, siempre se realiza un nuevo renderizado........
El estado no puede modificar nada que no este vinculado directamente al nodo que lo contiene...
No hacer SetState si no hace falta.......


en estilos en lineas no se puede reutilizar codigo y este lo hace mucho mas extenso.


sideEffects

Consecuencia de otra accion
Se Ejecutan despues de la accion principal de
Implicaciones externas








